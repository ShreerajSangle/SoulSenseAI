I want to completely rebuild and restructure the SoulSenseAI project from scratch using a new, clean architecture while retaining the exact same UI design, workflow, and user experience. The system should function identically to the current version, but with a new backend stack written entirely in Python (FastAPI), and a React frontend built using Vite or Create React App.

🧹 Step 1: Clean Up – Remove All Old Files
First, delete every existing file, folder, and asset in the current project directory, including:

All .ts, .tsx, .js, .jsx, server/, api/, build/, dist/, node_modules/

TypeScript/Node.js backend files (server/index.ts, routes-clean.ts, etc.)

Old SQLite files, .eslintrc, vite.config.*, yarn.lock, and leftover temp/test files

We are starting with zero legacy files to ensure a clean Python rebuild.

🛠️ Step 2: New Architecture
✅ Frontend (unchanged layout/UI)
Framework: React

Build: CRA or Vite

Language: JS or TSX (same as current)

Design: Must retain current UI (persona sidebar, chat view, profile, goals, etc.)

Behavior: Chat should occupy full screen per persona, without layout bugs

Quick Replies and Guided Prompts must remain contextual and non-robotic

✅ Backend (new)
Language: Python

Framework: FastAPI

Environment: Replit-compatible

Endpoint Design:

/chat/{persona} – Handle messages and memory

/journal/, /goals/, /profile/, /breathing/, /analytics/

Include CORS, logging, async support, and rate limiting

Store all data in PostgreSQL

✅ LLMs
Primary: Claude 3 via OpenRouter

Secondary: Mixtral via OpenRouter

Use header auth:
Authorization: Bearer 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

Add model-switching logic based on persona or input length

🧾 Step 3: PostgreSQL Setup
Use PostgreSQL (no SQLite)

Tables to include:

users, sessions, chats, goals, journal_entries

breathing_logs, mood_entries, analytics, persona_memory

Use SQLAlchemy or asyncpg

Auto-initialize schema

Store:

Chat history

Quick reply usage

Daily emotion logs

Journal summaries

Persona-specific settings and behavior

🔄 Step 4: Persona Separation
Each persona must:

Have its own config and response logic

Use its own LLM behavior file (e.g., maya_prompt.py, marcus_prompt.py)

Start with unique intro like:

Maya: “Let’s begin with 3 grounding breaths…”

Marcus: “Ready to level up? Let’s set a SMART goal today.”

Use Claude for Alex/Sarah; use Mixtral for Maya/Marcus

Allow smart prompt routing

📊 Step 5: Profile Analytics
Under /profile:

Show:

Total sessions

Current streak

Goals completed

Most active times

Avg. mood score

Recent reflections

Data must be fetched from PostgreSQL in real-time

🔑 Step 6: Environment Config
.env should contain:

env
Copy
Edit
OPENROUTER_API_KEY=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
DATABASE_URL=postgresql+asyncpg://<user>:<password>@<host>:5432/soulsense
✅ Step 7: Running the App
Once restructured, the system must:

Run backend with:

lua
Copy
Edit
uvicorn backend.main:app --reload --port=8000
Run frontend with:

arduino
Copy
Edit
cd client
npm install
npm run dev
Everything must function as it does today — with the same UI, same endpoints, and same features. The only difference: it's now clean, Pythonic, scalable, and fully React-powered.

Optional (if desired):
Add Redis for persona memory

Add error logging middleware

Enable persona-specific analytics in DB

Allow persona chat switching without logout