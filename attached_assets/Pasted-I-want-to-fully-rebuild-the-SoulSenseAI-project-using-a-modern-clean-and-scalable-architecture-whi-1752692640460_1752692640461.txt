I want to fully rebuild the SoulSenseAI project using a modern, clean, and scalable architecture while preserving the same persona logic, workflows, chat flows, memory structure, and emotional modules as implemented in the original SoulSenseAI project. The updated version should have a React frontend (JavaScript only) and a Python FastAPI backend — no .ts (TypeScript) files should remain. Every functionality from the current system (persona config loading, prompt memory, breathing tools, emotion tracking, journaling, UI personas, mood-based responses, and full chat logic) should be preserved and migrated into the new stack without breaking any part of the experience.

The system will have four separate personas — Maya, Marcus, Dr. Sarah, and Alex — each with their own chat route, system prompt, emotional logic, config file, UI theme, and backend handler. When a user opens any one persona, their chat window should go full screen, styled visually to reflect their identity (e.g., Maya’s calming lavender palette with yoga icons). Each persona loads only their own system context, voice tone, and memory logic — responses from one persona should not overlap with another.

Each persona module will point to the same LLM (e.g., Claude, Mixtral, or GPT) but with dynamically loaded system prompts and memory embeddings. Use the same route structure as the original project (e.g., /chat/maya, /chat/sarah, etc.) and preserve the original chat history architecture, UI triggers, emotion mapping, and reflection loops. All frontend widgets like breathing exercises, goal setting, chakra UI elements, and journaling tools must be retained but redesigned using React components, and they should now include a clear cancel or close button so users can exit these overlays gracefully.

The backend will use Python + FastAPI and connect to a SQLite database (or optionally Firebase) to persist persona-based memory, chat history, breath sessions, journaling, goals, and emotional state logs. ORM frameworks like SQLAlchemy should be used to model tables such as users, persona_logs, journal_entries, goals, and breathing_sessions, profile section with each entry tagged by persona and user ID. The same emotional logic, chakra mapping, affirmations, and journal reflection prompts from the original system should be preserved and made modular.

Finally, scan the project, delete all unnecessary files and folders not related to the working SoulSense system. The final system should preserve all the persona workflows, modules, emotion logic, and therapeutic flows of the original SoulSenseAI (1).zip project while delivering a clean React + Python-based rebuild with a working backend database and isolated persona logic.
