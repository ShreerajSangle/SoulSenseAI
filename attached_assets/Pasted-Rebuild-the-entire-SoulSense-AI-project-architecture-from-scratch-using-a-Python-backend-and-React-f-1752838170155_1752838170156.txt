Rebuild the entire SoulSense AI project architecture from scratch using a Python backend and React frontend, replacing the existing TypeScript/Express server. The entire backend should be restructured using FastAPI (Python) without altering the existing frontend visuals, user experience, or chat flows. All current functionalities, including the 4 individual persona chat panels (Maya, Marcus, Alex, Dr. Sarah), journal entries, breathing exercises, goal tracking, mood logging, session analytics, and profile overviews, must be replicated exactly as they work now.

✔️ Key Requirements:

Frontend must remain React-based (JSX/TSX files) with the same routing, styling, and layout.

Backend must be fully replaced with Python (FastAPI).

All endpoints, logic, and database connections currently handled in TypeScript should now be implemented in Python.

Replace Express routes with FastAPI endpoints under /api/ structure (e.g. /api/chat, /api/journal, /api/goals).

Use PostgreSQL or SQLite with SQLAlchemy or Tortoise ORM for database models and async operations.

Replicate session tracking, emotion logs, goal progress, journal summaries, and user profiles in the Python database logic.

Store all chat data, journal entries, goals, breathing activity, emotional patterns, and session history.

Ensure frontend fetch calls, analytics dashboard, and all UI components are untouched and continue to function exactly as before.

Completely remove the TypeScript server files and related build logic, once Python backend is confirmed stable and equivalent.

🎯 Final Goal:

A fully working SoulSense system with:

🧠 Python FastAPI backend

💻 React frontend (unchanged visuals and UX)

🗂️ Full database integration (chat, analytics, profiles)

🎯 Identical experience as the original app — only backend stack has changed, not the behavior

This new structure must mirror the original project perfectly, offering a clean and scalable backend in Python for easier LLM integration and long-term maintainability.