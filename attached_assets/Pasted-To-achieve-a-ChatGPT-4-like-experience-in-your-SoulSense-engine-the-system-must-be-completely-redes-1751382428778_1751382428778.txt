To achieve a ChatGPT-4–like experience in your SoulSense engine, the system must be completely redesigned into a smart, emotionally aware, memory-driven conversational agent using Claude 3 Haiku via OpenRouter. The first step is to replace any old or placeholder model logic by fully integrating Claude 3 using a custom claude_chat.py module. This model offers near-GPT-4 fluency and excels in emotional tone, making it ideal for Replika-style therapeutic interactions. Next, implement a prompt_builder.py module that dynamically constructs intelligent prompts. This builder will take the current user input, short-term memory of the last 5–10 messages, detected emotion, and selected persona, then output a structured, context-rich prompt like: “You are Dr. Sarah, a kind and empathetic AI therapist. The user is feeling anxious today and has asked for career guidance. Keep the tone warm and structured. Ask follow-up questions and guide them with clarity.” This level of system priming is what gives ChatGPT its clarity and depth. Alongside this, integrate short-term memory using a simple session_memory.json or in-RAM structure, allowing the assistant to remember recent conversation turns and maintain context. Then, plug in your text_emotion.py to analyze the user's emotional tone and inject that awareness into the system prompt, enabling tone adjustments (gentle, firm, curious) depending on whether the user is anxious, confused, or hopeful. Next, add an intent classifier that parses user goals—for example, if a user says, “I want career guidance,” the system should identify the intent as “guidance,” the topic as “career,” and inject this into the prompt to trigger a structured, follow-up-driven response. To avoid dull repetition, build a response variation checker that compares each new Claude reply with previous ones and regenerates if it matches too closely. This ensures freshness and variety in dialogue. For vague queries like “I need help,” your assistant should respond using pre-written structured templates loaded from a templates/ folder (e.g., “career_guidance.yaml”), containing follow-up questions that emulate the style of ChatGPT’s “Basic Info” prompts. Finally, unify all of this into your controller.py where the logic flow is: capture user input → detect emotion and intent → retrieve memory and persona → build prompt → call Claude → check for repetition → log response to memory → display the output. With these changes, SoulSense will not only match ChatGPT-4 in intelligence and tone but also deliver deeply personalized, emotionally aligned responses that feel as human and helpful as Replika.